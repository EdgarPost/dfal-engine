// import { Node } from './types';
// import { getMarkdownFiles } from './utils/getMarkdownFiles';
// import { fileToNode } from './utils/markdownToNode';

// // const main = async (): Promise<void> => {
// //   const files = await getMarkdownFileggVGs('templates/**/*.md');

// //   const groups: Record<string, Node[]> = {};

// //   for (const file of files) {
// //     const result = await fileToNode(file);
// //     if (!(result.type in groups)) {
// //       groups[result.type] = [];
// //     }

// //     groups[result.type].push(result);
// //   }
// // };

// // eslint-disable-next-line @typescript-eslint/no-floating-promises
// (async function () {
//   await main();
// })();
